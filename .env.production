# Production build (GitHub Pages). Points at the backend exposed via a tunnel.
# Localtunnel URLs change each restart — update this and redeploy when it changes.
VITE_API_BASE=https://slick-results-cross.loca.lt
