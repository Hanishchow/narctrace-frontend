# narctrace-frontend environment (copy to .env.local; not committed)
# --- Port sync contract (MUST match backend .env.example) ---
# Dev backend origin. In prod set to the deployed backend HTTPS URL.
VITE_API_BASE=http://localhost:8000

# Vite dev server runs on 5173 (see vite.config.ts). This must be present in
# the backend CORS_ORIGINS list.
